import { ViewFormatPipe } from './view-format.pipe';

describe('ViewFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ViewFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
